apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: saravia
spec:
  replicas: 3
  selector:
    matchLabels: { app: auth-service }
  template:
    metadata:
      labels: { app: auth-service }
    spec:
      containers:
        - name: auth-service
          image: ghcr.io/saravia/auth-service:latest
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef: { name: saravia-secrets }
          readinessProbe:
            httpGet: { path: '/', port: 3000 }
          livenessProbe:
            httpGet: { path: '/', port: 3000 }
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: saravia
spec:
  selector: { app: auth-service }
  ports:
    - port: 80
      targetPort: 3000
