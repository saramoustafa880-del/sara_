apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service
  namespace: saravia
spec:
  replicas: 3
  selector:
    matchLabels: { app: booking-service }
  template:
    metadata:
      labels: { app: booking-service }
    spec:
      containers:
        - name: booking-service
          image: ghcr.io/saravia/booking-service:latest
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef: { name: saravia-secrets }
          readinessProbe:
            httpGet: { path: '/', port: 3000 }
          livenessProbe:
            httpGet: { path: '/', port: 3000 }
---
apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: saravia
spec:
  selector: { app: booking-service }
  ports:
    - port: 80
      targetPort: 3000
