apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-service
  namespace: saravia
spec:
  replicas: 3
  selector:
    matchLabels: { app: ai-service }
  template:
    metadata:
      labels: { app: ai-service }
    spec:
      containers:
        - name: ai-service
          image: ghcr.io/saravia/ai-service:latest
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef: { name: saravia-secrets }
          readinessProbe:
            httpGet: { path: '/', port: 3000 }
          livenessProbe:
            httpGet: { path: '/', port: 3000 }
---
apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: saravia
spec:
  selector: { app: ai-service }
  ports:
    - port: 80
      targetPort: 3000
